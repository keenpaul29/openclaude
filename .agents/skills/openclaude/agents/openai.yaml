interface:
  display_name: "Openclaude"
  short_description: "Repo-specific patterns and workflows for openclaude"
  default_prompt: "Use the openclaude repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true